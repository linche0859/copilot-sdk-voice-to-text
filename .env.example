# Environment template for deployment (replace values before use)
# Zeabur will typically inject `PORT` automatically.

# Application
PORT=3000
NODE_ENV=production

# Copilot SDK / CLI
# If you run Copilot CLI inside the container and have a non-interactive token,
# set `COPILOT_TOKEN`. Otherwise perform `copilot login` interactively after container start
# or use a remote Copilot CLI server and set `COPILOT_CLI_URL`.
COPILOT_TOKEN=
COPILOT_CLI_URL=

# MCP server API key (if you configure MCP servers that rely on an API key)
COPILOT_MCP_API_KEY=

# GitHub token for MCP or GitHub API access (if needed)
GITHUB_TOKEN=

GITHUB_PAT_TOKEN=